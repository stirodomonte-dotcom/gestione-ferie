const CACHE='gf-v1';
const URLS=['/login.html','/dashboard.html','/admin.html','/css/app.css','/js/common.js','/js/offline.js','/manifest.json','https://cdn.jsdelivr.net/npm/fullcalendar@6.1.9/index.global.min.css','https://cdn.jsdelivr.net/npm/fullcalendar@6.1.9/index.global.min.js'];
self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>Promise.allSettled(URLS.map(u=>c.add(u).catch(()=>{}))).then(()=>self.skipWaiting())));});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()));});
self.addEventListener('fetch',e=>{const url=new URL(e.request.url);
  if(e.request.method==='GET'&&!url.pathname.startsWith('/api/')){e.respondWith(caches.match(e.request).then(cached=>{const net=fetch(e.request).then(r=>{if(r.ok){const cl=r.clone();caches.open(CACHE).then(c=>c.put(e.request,cl));}return r;});return cached||net;}));return;}
  if(e.request.method==='GET'&&url.pathname.startsWith('/api/')){e.respondWith(fetch(e.request).then(r=>{if(r.ok){const cl=r.clone();caches.open(CACHE).then(c=>c.put(e.request,cl));}return r;}).catch(()=>caches.match(e.request).then(c=>c||new Response(JSON.stringify({error:'offline'}),{headers:{'Content-Type':'application/json'}}))));return;}
});
self.addEventListener('sync',e=>{if(e.tag==='sync-richieste')e.waitUntil(syncPending());});
function openIDB(){return new Promise((res,rej)=>{const req=indexedDB.open('gf-offline',1);req.onupgradeneeded=e=>{const db=e.target.result;if(!db.objectStoreNames.contains('sync_queue'))db.createObjectStore('sync_queue',{keyPath:'id',autoIncrement:true});};req.onsuccess=e=>res(e.target.result);req.onerror=e=>rej(e.target.error);});}
async function syncPending(){const db=await openIDB();const items=await new Promise((res,rej)=>{const tx=db.transaction('sync_queue','readonly');const req=tx.objectStore('sync_queue').getAll();req.onsuccess=e=>res(e.target.result);req.onerror=e=>rej(e.target.error);});for(const item of items){try{const r=await fetch(item.url,{method:item.method,headers:item.headers,body:item.body});if(r.ok){await new Promise((res,rej)=>{const tx=db.transaction('sync_queue','readwrite');tx.objectStore('sync_queue').delete(item.id);tx.oncomplete=res;tx.onerror=rej;});const clients=await self.clients.matchAll();clients.forEach(c=>c.postMessage({type:'SYNC_OK',item}));}}catch{}}}
